# Rustle UI 优化计划

## 问题分析

当前界面存在以下改进空间：

### 视觉设计问题
1. **颜色方案过于简陋**：使用基础颜色，缺乏现代感
   - 离线用户背景用黄色，视觉冲击过强
   - 在线用户用绿色，与整体风格不搭
   - 缺乏配套的中性背景色和强调色

2. **消息气泡设计不够精致**
   - 气泡边框简单，缺乏视觉层次
   - 没有阴影效果，显得平面化
   - 不同状态的视觉区分度不足

3. **用户界面缺乏视觉层次**
   - 标题和普通文本区分不够
   - 按钮样式不够突出
   - 输入框样式简陋

4. **整体布局显得不够专业**
   - 间距和填充不够精心设计
   - 组件间的对齐度需要改进
   - 没有clear的视觉分离

## 优化方案

### 第一阶段：颜色方案和主题（优先级: 高）

#### 采用专业的配色方案
- **主色**：深蓝色 `#2563eb` - 用于主要交互元素
- **次色**：青色 `#0891b2` - 用于在线状态和强调
- **背景**：浅灰色 `#f8fafc` - 主界面背景
- **文本**：深灰色 `#1e293b` - 主文本
- **边框**：`#e2e8f0` - 轻微分离线

#### 在线/离线状态指示
- **在线**：青色背景 + 青色点缀 `#06b6d4` (柔和)
- **离线**：浅灰色 `#cbd5e1` (不突出)
- **未读**：蓝色点或阴影 `#3b82f6`

### 第二阶段：组件样式升级（优先级: 高）

#### 消息气泡设计
- 添加投影效果：`shadow(0, 2px, 8px, rgba(0,0,0,0.1))`
- 改进圆角：使用 8-10px 圆角
- 自己的消息：蓝色背景 + 白色文字
- 对方的消息：白色背景 + 深灰色文字 + 浅灰色边框
- 添加间距和内边距改进可读性

#### 用户列表项
- 悬停效果：轻微背景色变化
- 选中效果：左边框指示 + 背景色
- 在线指示符：小圆点 + 颜色
- 未读标记：右上角红点或蓝点

#### 按钮风格
- 主要按钮：蓝色背景 + 悬停深化 + 阴影
- 次要按钮：边框风格
- 鼠标悬停时添加亮度变化
- 提高按钮的视觉反馈

### 第三阶段：布局和间距优化（优先级: 中）

#### 整体间距
- 增加元素间的一致间距
- 改进边距和填充的比例
- 创建清晰的视觉节奏

#### 消息区域
- 改进消息间距（增大到 10-12px）
- 改进时间戳的显示方式（使用更小、更淡的字体）
- 改进状态标签的显示位置和风格

### 第四阶段：排版优化（优先级: 中）

#### 字体和文字大小
- 标题加粗处理更清晰
- 改进行高和字间距
- 优化各个层级的文字大小对比

#### 特殊文本效果
- 时间戳：更小且更淡
- 状态提示：斜体或特殊颜色
- 链接文本：明确的下划线

## 实施步骤

### ✅ 步骤 1：定义主题常量
在 `ui.rs` 顶部定义色值常量，便于管理和修改

### ✅ 步骤 2：更新用户列表项样式
- 改进在线/离线用户的视觉表示
- 优化选中状态的样式
- 添加左边框指示器

### ✅ 步骤 3：优化消息气泡样式
- 添加阴影效果
- 改进背景色和文字色对比
- 优化气泡尺寸和间距

### ✅ 步骤 4：改进输入区域
- 优化按钮样式
- 改进输入框样式
- 增加视觉反馈

### ✅ 步骤 5：优化其他 UI 元素
- 改进窗口和对话框样式
- 优化菜单和设置界面
- 统一全局风格

### ✅ 步骤 6：测试和微调
- 确保所有功能正常
- 验证颜色对比度和可读性
- 收集反馈

## 预期效果

✨ 更现代、更专业的视觉呈现
✨ 更清晰的信息层次
✨ 更好的用户交互反馈
✨ 整体更美观和精致的应用

## 技术实现说明

- 使用 egui 的 `Color32` 定义颜色常量
- 使用 `rounding()` 和 `stroke()` 改进形状
- 使用 `shadow()` 添加阴影（若 egui 支持）
- 使用 `hover` 和 `interaction` 改进交互反馈
